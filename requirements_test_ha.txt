# Home Assistant integration tests (tests/integration). Requires Python 3.14.
# pytest-homeassistant-custom-component pins a matching Home Assistant + pytest.
-r requirements_test.txt
pytest-homeassistant-custom-component
pyyaml>=5.4 # not directly required, pinned by Snyk to avoid a vulnerability
